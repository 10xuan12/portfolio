# 技能雷達圖邏輯優化

## ❌ 原本的問題

### 問題1：關鍵字重複導致不合理的雷達圖

**範例**：一個只會 Python 的學生

**原本的邏輯**：
```
作品標籤：Python, R, Git

匹配結果：
- 後端開發（Python ✓）→ 95分 ❌
- 資料分析（Python ✓, R ✓）→ 85分 ❌
- 數學統計（R ✓）→ 45分 ❌
- 專案管理（Git ✓）→ 85分 ❌

雷達圖：4個類別都有高分！
```

**問題**：
- 同一個作品被計算 4 次
- 看起來像是全能型人才（實際上只是關鍵字重複）
- 不符合實際專業分布

---

## ✅ 優化方案

### 方案1：清理重複關鍵字

**原則**：每個關鍵字只屬於一個最相關的類別

#### 移除的重複關鍵字

| 關鍵字 | 原本出現在 | 現在只保留在 |
|--------|-----------|-------------|
| Python | 後端開發、資料分析 | **資料分析** |
| R | 資料分析、數學統計 | **資料分析** |
| Git | 專案管理、雲端技術 | **雲端技術** |
| MATLAB | 機器人學、數學統計 | **數學統計** |
| 統計 | 資料分析、數學統計 | 移除（用更精確的詞） |
| Research Methods | 心理學、數位媒體 | **心理學** |
| 社群媒體 | 數位行銷、數位媒體 | **數位行銷** |
| 3D建模 | 3D設計、建築營建 | **3D設計** |
| AutoCAD | 3D設計、建築營建 | **建築營建** |

#### 優化後的類別定義

```php
'前端開發' => ['HTML', 'CSS', 'JavaScript', 'React', 'Vue', 'Angular', 'TypeScript', 'Next.js', 'Webpack', 'Vite', '網頁設計']
// 移除：無

'後端開發' => ['Node.js', 'PHP', 'Java', 'C#', 'Go', 'Ruby', 'Rust', '後端', 'API', 'REST', 'GraphQL', 'Spring', 'Django', 'Laravel', 'Express']
// 移除：Python（歸到資料分析）

'資料分析' => ['Python', 'R', 'Excel', 'PowerBI', 'Tableau', 'Pandas', 'NumPy', '數據分析', '資料分析', '商業分析', 'Excel試算表', 'PowerBI商業智慧']
// 保留：Python, R（這是主要領域）
// 移除：統計（太泛用）

'數學統計' => ['數學建模', '最佳化', '線性規劃', '統計學', '迴歸分析', 'MATLAB', 'SPSS', '統計分析', '數值分析']
// 保留：MATLAB（數學計算為主）
// 移除：R（歸到資料分析）

'專案管理' => ['專案管理', 'Agile', 'Scrum', 'Kanban', 'Jira', 'Trello', '團隊協作', '任務追蹤']
// 移除：Git（歸到雲端技術）

'雲端技術' => ['AWS', 'Docker', 'Kubernetes', 'DevOps', 'CI/CD', 'Git', '版本控制', 'Jenkins', 'Linux']
// 新增：Git, 版本控制（更相關）

'數位媒體' => ['數位媒體', '內容創作', '影音製作', '直播', '媒體企劃', '新聞', '傳播']
// 移除：社群媒體行銷（歸到數位行銷）

'數位行銷' => ['行銷', 'SEO', 'Google Analytics', 'Google Ads', '社群媒體', '社群媒體行銷', '內容行銷', '數位行銷', '行銷策略']
// 保留：社群媒體（行銷為主）
```

---

### 方案2：限制每個作品最多匹配 3 個類別

**新邏輯**：

```php
// 第一階段：計算所有類別的匹配分數
foreach ($skillCategories as $category => $keywords) {
    $matchScore = 計算匹配分數();
    if ($matchScore > 0) {
        $portfolioMatches[$category] = $matchScore;
    }
}

// 第二階段：只取前 3 名
arsort($portfolioMatches);  // 按分數排序
$topMatches = array_slice($portfolioMatches, 0, 3);  // 只取前3個

// 第三階段：只對前 3 名計分
foreach ($topMatches as $category => $matchData) {
    $skillScores[$category]['portfolio_count']++;
    // ...
}
```

---

## 📊 優化前後對比

### 範例：一個 Python 數據分析作品

**作品資料**：
```
標籤：Python, R, PowerBI, Excel, Git
描述：使用 Python 進行數據分析
```

#### ❌ 優化前

```
匹配結果（所有匹配的都計分）：
- 後端開發（Python ✓）→ 匹配分數 2
- 資料分析（Python ✓, R ✓, PowerBI ✓, Excel ✓）→ 匹配分數 8
- 數學統計（R ✓）→ 匹配分數 2
- 專案管理（Git ✓）→ 匹配分數 2
- 雲端技術（Git ✓）→ 匹配分數 2

結果：5個類別都加分！❌
```

#### ✅ 優化後

```
第一階段 - 計算所有匹配分數：
- 資料分析：8分（最高）
- 後端開發：2分
- 數學統計：0分（R已移除）
- 專案管理：0分（Git已移除）
- 雲端技術：2分

第二階段 - 排序並取前3名：
1. 資料分析：8分 ✅
2. 後端開發：2分 ✅
3. 雲端技術：2分 ✅

第三階段 - 只對前3名計分：
結果：只有3個類別加分！✅
```

---

## 🎯 實際效果

### 案例1：資安專家

**作品標籤**：`網路安全, 滲透測試, Python, 弱點評估`

#### 優化前：
```
雷達圖：
- 網路安全：95分
- 後端開發：85分（Python）❌
- 資料分析：60分（Python）❌
```

#### 優化後：
```
匹配分數：
- 網路安全：8分（最高）
- 後端開發：2分
- 資料分析：2分

只取前3名 → 實際上只有1個高分：
雷達圖：
- 網路安全：95分 ✅
- 後端開發：20分（輔助）
- 資料分析：15分（輔助）
```

---

### 案例2：平面設計師

**作品標籤**：`Photoshop, Illustrator, Logo設計, 品牌識別`

#### 優化前：
```
雷達圖：
- 平面設計：90分
- UI/UX設計：45分（設計相關）❌ 不該這麼高
```

#### 優化後：
```
匹配分數：
- 平面設計：8分（最高）
- UI/UX設計：0分（關鍵字已清理）

雷達圖：
- 平面設計：90分 ✅（專精）
```

---

### 案例3：全端工程師

**作品1**：`React, TypeScript, CSS`
**作品2**：`Node.js, Express, MongoDB`
**作品3**：`Docker, AWS, CI/CD`

#### 優化前：
```
雷達圖：
- 前端開發：85分
- 後端開發：80分
- 資料庫：70分
- 雲端技術：75分
- 專案管理：60分（Git）❌ 不該出現
```

#### 優化後：
```
作品1 匹配：前端開發（6分）✅
作品2 匹配：後端開發（4分）、資料庫（2分）✅
作品3 匹配：雲端技術（6分）✅

雷達圖：
- 前端開發：85分 ✅
- 後端開發：80分 ✅
- 資料庫：70分 ✅
- 雲端技術：75分 ✅
（只有4個類別，更合理）
```

---

## 📈 優化效果

### 優化前的問題

| 問題 | 原因 | 影響 |
|------|------|------|
| 類別過多 | 關鍵字重複 | 雷達圖有 7-9 個角 |
| 分數虛高 | 重複計算 | 看起來很厲害但不真實 |
| 不合邏輯 | 跨領域匹配 | 工程師有心理學分數？ |

### 優化後的改善

| 改善 | 方法 | 效果 |
|------|------|------|
| 類別精簡 | 限制前3名 | 雷達圖 2-4 個角 |
| 分數真實 | 移除重複 | 反映真實專長 |
| 邏輯合理 | 關鍵字專屬 | 專業領域清晰 |

---

## 🎨 視覺效果對比

### 優化前（不合理）
```
        後端(95)
          /\
    前端 /  \ 資料庫(85)
   (70) |    |
        |    |
   專案  \  / 資料分析(80)
   (85)  \/
     心理學(65) ← 為什麼會有？❌
```
**問題**：一個工程師有心理學分數？

### 優化後（合理）
```
        資料分析(90)
           /\
          /  \
    前端 /    \ 後端
   (65) |      | (70)
        |      |
         \    /
          \  /
           \/
        雲端(55)
```
**改善**：只有相關的技術領域，邏輯清晰

---

## 🔧 技術實作細節

### 改進1：關鍵字去重

**移除的重複關鍵字**：
- `Python`：從「後端開發」移除，只保留在「資料分析」
- `R`：從「數學統計」移除，只保留在「資料分析」
- `Git`：從「專案管理」移除，只保留在「雲端技術」
- `MATLAB`：從「機器人學」移除，只保留在「數學統計」
- `Simulink`：從「機器人學」移除，只保留在「數學統計」
- `統計`：從「資料分析」移除（用更精確的「統計學」）
- `Research Methods`：從「數位媒體」移除，只保留在「心理學」
- `社群媒體行銷`：從「數位媒體」移除，只保留在「數位行銷」
- `3D建模`：從「建築營建」移除，只保留在「3D設計」

### 改進2：Top-3 匹配邏輯

**新增代碼**：

```php
// 第一階段：計算所有類別的匹配分數
$portfolioMatches = [];
foreach ($skillCategories as $category => $keywords) {
    $matchScore = 計算匹配分數();
    if ($matchScore > 0) {
        $portfolioMatches[$category] = $matchScore;
    }
}

// 第二階段：排序並只取前3名
arsort($portfolioMatches);
$topMatches = array_slice($portfolioMatches, 0, 3, true);

// 第三階段：只對前3名計分
foreach ($topMatches as $category => $matchData) {
    $skillScores[$category]['portfolio_count']++;
    // ...
}
```

---

## 📊 測試案例

### 測試1：資安專家（張志強）

**作品**：網路安全滲透測試工具
**標籤**：`網路安全, 滲透測試, Python, 弱點評估`

**匹配計算**：
```
網路安全：8分（網路安全✓, 滲透測試✓, 弱點評估✓）
資料分析：2分（Python✓）
其他：0分

Top 3：網路安全(8), 資料分析(2)
```

**雷達圖**：
```
- 網路安全：95分 ⭐⭐⭐（專家）
- 資料分析：25分（輔助技能）
```

**結論**：✅ 合理！清楚顯示主要專長是資安

---

### 測試2：平面設計師（徐靜雯）

**作品**：品牌識別設計系統
**標籤**：`品牌識別, Logo設計, 視覺識別, Adobe創意套件, UI/UX設計`

**匹配計算**：
```
平面設計：8分（品牌識別✓, Logo✓, 視覺識別✓, Adobe✓）
UI/UX設計：2分（UI/UX設計✓）
其他：0分

Top 3：平面設計(8), UI/UX設計(2)
```

**雷達圖**：
```
- 平面設計：88分 ⭐⭐⭐（專家）
- UI/UX設計：35分（輔助技能）
```

**結論**：✅ 合理！主要是平面設計，UI/UX是輔助

---

### 測試3：全端工程師（黃玟瑄）

**作品1**：電商數據分析
**標籤**：`Python, PowerBI, 數據分析, 電商, 用戶行為`

**匹配計算**：
```
資料分析：8分（Python✓, PowerBI✓, 數據分析✓）
電商商業：4分（電商✓, 用戶行為✓）
其他：0分

Top 3：資料分析(8), 電商商業(4)
```

**作品2**：社群媒體行銷策略
**標籤**：`社群媒體, 行銷策略, 內容規劃, 品牌推廣`

**匹配計算**：
```
數位行銷：8分（社群媒體✓, 行銷策略✓, 內容規劃✓, 品牌推廣✓）

Top 3：數位行銷(8)
```

**作品3**：學生系統UI/UX設計
**標籤**：`UI/UX設計, 響應式設計, 無障礙設計, 使用者研究`

**匹配計算**：
```
UI/UX設計：8分（UI/UX✓, 響應式✓, 無障礙✓, 使用者研究✓）
前端開發：2分（響應式✓）

Top 3：UI/UX設計(8), 前端開發(2)
```

**作品4**：Python爬蟲程式
**標籤**：`Python, 爬蟲, 自動化, 數據收集, Selenium`

**匹配計算**：
```
爬蟲自動化：8分（爬蟲✓, 自動化✓, 數據收集✓, Selenium✓）
資料分析：2分（Python✓）

Top 3：爬蟲自動化(8), 資料分析(2)
```

**作品5**：專案管理系統
**標籤**：`React, Node.js, 專案管理, 團隊協作, 任務追蹤`

**匹配計算**：
```
前端開發：2分（React✓）
後端開發：2分（Node.js✓）
專案管理：6分（專案管理✓, 團隊協作✓, 任務追蹤✓）

Top 3：專案管理(6), 前端開發(2), 後端開發(2)
```

**最終雷達圖**（合併所有作品）：
```
- 資料分析：82分 ⭐⭐（2個作品強相關）
- 數位行銷：75分 ⭐⭐（1個作品強相關）
- UI/UX設計：68分 ⭐（1個作品強相關）
- 爬蟲自動化：65分 ⭐（1個作品強相關）
- 專案管理：70分 ⭐（1個作品強相關）
- 前端開發：45分（2個作品弱相關）
- 後端開發：40分（1個作品弱相關）
- 電商商業：35分（1個作品弱相關）
```

**結論**：✅ 合理！顯示多元技能，但每個領域都有明確依據

---

## ⚙️ 優化參數

### 可調整的參數

```php
// 每個作品最多匹配的類別數
$maxCategoriesPerPortfolio = 3;  // 可調整為 2-5

// 標籤匹配權重
$tagMatchWeight = 2;  // 標籤匹配的分數

// 文字匹配權重
$textMatchWeight = 1;  // 標題/描述匹配的分數
```

---

## ✅ 優化效果

### 1. 更真實的專業分布
- ❌ 之前：9個類別都有分（不合理）
- ✅ 現在：2-5個類別有分（合理）

### 2. 更清晰的專長識別
- ❌ 之前：分數都很高，看不出專長
- ✅ 現在：主要專長明顯高於其他

### 3. 更符合邏輯
- ❌ 之前：工程師有心理學分數
- ✅ 現在：只顯示相關領域

### 4. 更好的比較性
- ❌ 之前：每個人都是全能型
- ✅ 現在：可以清楚看出專精領域

---

## 🚀 下一步

優化已完成！建議：

1. **測試實際效果**
   - 查看幾位不同領域學生的雷達圖
   - 確認是否更合理

2. **微調參數**
   - 如果還是覺得類別太多，可以改成 Top-2
   - 如果覺得太少，可以改成 Top-4

3. **收集反饋**
   - 學生是否覺得雷達圖更準確？
   - 企業是否更容易理解學生專長？

---

**版本**：4.0  
**更新日期**：2025-12-05  
**狀態**：✅ 已完成優化  
**改進**：清理重複關鍵字 + Top-3 匹配限制

