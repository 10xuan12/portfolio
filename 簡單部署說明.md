# 🎯 超簡單部署說明（圖解版）

## 問題是什麼？

**簡單來說：** Railway 上的圖片找不到，因為資料庫裡記錄的路徑是錯的。

### 📸 圖片路徑問題

```
❌ 錯誤的路徑（Railway 資料庫裡現在的樣子）：
/portfolio/uploads/portfolios/ecommerce-analysis.jpg

✅ 正確的路徑（應該要改成這樣）：
/uploads/portfolios/ecommerce-analysis.jpg

問題：多了 /portfolio/ 這一段！
```

## 🔧 怎麼修正？分 3 步驟

### 第 1 步：上傳程式碼到 Railway

這一步最簡單，在電腦上執行：

```bash
# 1. 打開 PowerShell 或 CMD
# 2. 切換到專案資料夾
cd D:\xampp\htdocs\portfolio

# 3. 執行這 3 行指令
git add .
git commit -m "修正圖片路徑問題"
git push
```

**完成！** Railway 會自動開始部署（等 3-5 分鐘）

---

### 第 2 步：修正 Railway 資料庫的路徑

這是**關鍵步驟**！您需要告訴 Railway 的資料庫：「圖片路徑要改一下」

#### 🌐 打開 Railway 網站

1. 開啟瀏覽器
2. 前往：https://railway.app
3. 登入您的帳號
4. 點選您的「portfolio」專案

#### 🗄️ 找到資料庫

在專案頁面中，您會看到幾個方塊（服務）：
- 📦 web（您的網站）
- 🗄️ MySQL（資料庫）← **點這個**

#### 📝 執行 SQL 指令

點進 MySQL 後，找到「**Query**」或「**Data**」標籤

然後**複製貼上**以下 4 段指令（一次全部貼上也可以）：

```sql
UPDATE portfolios 
SET cover_image = REPLACE(cover_image, '/portfolio/uploads/', '/uploads/') 
WHERE cover_image LIKE '/portfolio/uploads/%';

UPDATE portfolio_files 
SET file_path = REPLACE(file_path, '/portfolio/uploads/', '/uploads/') 
WHERE file_path LIKE '/portfolio/uploads/%';

UPDATE student_profiles 
SET avatar_url = REPLACE(avatar_url, '/portfolio/uploads/', '/uploads/') 
WHERE avatar_url LIKE '/portfolio/uploads/%';

UPDATE enterprise_profiles 
SET logo_url = REPLACE(logo_url, '/portfolio/uploads/', '/uploads/') 
WHERE logo_url LIKE '/portfolio/uploads/%';
```

然後按「**Execute**」或「**執行**」按鈕

**完成！** 資料庫路徑已修正

---

### 第 3 步：重新整理網站

1. 打開您的 Railway 網站
2. 按 `Ctrl + Shift + R`（強制重新整理）
3. 圖片應該就會顯示了！

---

## 🎨 視覺化流程圖

```
您的電腦 (本地)
    ↓
  git push
    ↓
  GitHub
    ↓ (自動)
  Railway 開始部署
    ↓
  程式碼已更新 ✅
    ↓
  但資料庫路徑還是舊的 ❌
    ↓
  【您需要手動執行 SQL】← 這就是第 2 步
    ↓
  資料庫路徑更新 ✅
    ↓
  圖片顯示正常！🎉
```

---

## ❓ 找不到 Railway 的 Query 功能？

### 方法 A：用 Railway 網站介面

1. Railway Dashboard → 點 MySQL 服務
2. 切換到「**Data**」標籤
3. 看到一個可以輸入 SQL 的地方
4. 貼上 SQL → 執行

### 方法 B：用外部工具（如果您會用）

可以使用：
- **phpMyAdmin**
- **MySQL Workbench**
- **TablePlus**
- **DBeaver**

連線資訊在 Railway MySQL 服務的「**Connect**」標籤裡

---

## 🆘 還是不懂嗎？

### 最最最簡單的說法：

1. **上傳程式碼**（在電腦執行 `git push`）
2. **等 Railway 部署完成**（等 3 分鐘）
3. **告訴 Railway 資料庫改路徑**（在網站上執行那段 SQL）
4. **重新整理網站**（看圖片是否顯示）

### 那段 SQL 到底在做什麼？

就是告訴資料庫：

> 「把所有圖片路徑中的 `/portfolio/uploads/` 改成 `/uploads/`」

就這麼簡單！

---

## 📞 需要幫助？

如果您：
- ❌ 找不到 Railway 的 Query 功能
- ❌ 不知道怎麼執行 SQL
- ❌ 執行後還是沒有圖片

**請告訴我卡在哪一步**，我會用更簡單的方式幫您！

比如：
- 「我找不到 MySQL 服務在哪裡」
- 「我不知道怎麼貼上 SQL」
- 「執行後出現錯誤訊息」

---

**記住：您只需要做 3 件事**
1. ✅ git push（上傳）
2. ✅ 執行 SQL（改路徑）
3. ✅ 重新整理（看結果）

就這樣！🎉

